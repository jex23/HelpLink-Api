# Flask Configuration
SECRET_KEY=your-secret-key-here

# Database Configuration
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=3306
DB_NAME=helplink

# R2 Storage Configuration (Cloudflare R2)
r2_access_key=your_r2_access_key
r2_secret_key=your_r2_secret_key
r2_endpoint=https://your-account-id.r2.cloudflarestorage.com
r2_bucket_name=your_bucket_name

# SMTP Email Configuration
# For Gmail SSL (port 465): Use SMTP_ENCRYPTION=ssl
# For Gmail TLS (port 587): Use SMTP_ENCRYPTION=tls
# For SendGrid: smtp.sendgrid.net / port 587 / SMTP_ENCRYPTION=tls
# For Amazon SES: email-smtp.region.amazonaws.com / port 587 / SMTP_ENCRYPTION=tls
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_ENCRYPTION=ssl
SMTP_FROM_ADDRESS=your-email@gmail.com
SMTP_FROM_NAME="HelpLink Team"

# Notes:
# - For Gmail, you need to use an "App Password" instead of your regular password
#   Generate one at: https://myaccount.google.com/apppasswords
# - Port 465 uses SSL encryption (SMTP_ENCRYPTION=ssl)
# - Port 587 uses TLS encryption (SMTP_ENCRYPTION=tls)
# - For production, consider using a dedicated email service like SendGrid, Amazon SES, or Mailgun
